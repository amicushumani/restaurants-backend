version: 0.2

phases:
  install:
    commands:
      - echo Install phase
  pre_build:
    commands:   
      - echo pre build phase
  build:
    commands: 
      - echo build
  post_build:
    commands:  
      - echo post build